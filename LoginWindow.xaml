<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SoccerLink.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoccerLink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SoccerLink">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="#C5C5C5">
        <Border
            Width="360"
            CornerRadius="16"
            Background="#F91010"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Padding="28"
            >

            <StackPanel
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Spacing="32">

                <TextBlock Text="Logowanie" FontSize="20" HorizontalAlignment="Center" Foreground="#C5C5C5" FontWeight="Bold" />

                <StackPanel Spacing="6" HorizontalAlignment="Center">
                    <TextBlock Text="Adres email" Foreground="#C5C5C5" FontWeight="Bold" />
                    <Border Background="#C5C5C5" CornerRadius="8" Width="220" Height="40">
                        <TextBox x:Name="EmailTextBox" Background="Transparent" BorderThickness="0" Foreground="Black" Padding="12,8" PlaceholderText="email@przyklad.com" FontWeight="Bold" />
                    </Border>
                </StackPanel>

                <StackPanel Spacing="6" HorizontalAlignment="Center">
                    <TextBlock Text="Hasło" Foreground="#C5C5C5" FontWeight="Bold" />
                    <Border Background="#C5C5C5" CornerRadius="8" Width="220" Height="40">
                        <PasswordBox x:Name="PasswordBox" Background="Transparent" BorderThickness="0" Foreground="Black" Padding="12,8" PlaceholderText="Hasło" FontWeight="Bold" />
                    </Border>
                </StackPanel>

                <Grid HorizontalAlignment="Center" Margin="0,28,0,0">
                    <Border Background="#C5C5C5" CornerRadius="8" Width="180" Height="40">
                        <Button x:Name="LoginButton" Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" Click="LoginButton_Click">
                            <TextBlock Text="Zaloguj" Foreground="#F91010" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" />
                        </Button>
                    </Border>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</Window>
