<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SoccerLink.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoccerLink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SoccerLink">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="360"
            Spacing="32">

            <TextBlock Text="Logowanie" FontSize="20" HorizontalAlignment="Center" />

            <StackPanel Spacing="6">
                <TextBlock Text="Email" />
                <TextBox x:Name="EmailTextBox" PlaceholderText="email@przyklad.com" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Hasło" />
                <PasswordBox x:Name="PasswordBox" PlaceholderText="Hasło" />
            </StackPanel>

            <Button x:Name="LoginButton" Content="Zaloguj" HorizontalAlignment="Stretch" Background="#4FC3F7" Foreground="White" Click="LoginButton_Click" />

        </StackPanel>
    </Grid>
</Window>
