<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoccerLink.Views.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoccerLink.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#405861" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,20">
            <Button x:Name="BackButton" 
                    Content="⟵ Powrót" 
                    Click="BackButton_Click" 
                    HorizontalAlignment="Left"
                    Background="Transparent"
                    BorderThickness="0"
                    FontSize="18"
                    Foreground="#CDE6F8"
                    FontWeight="SemiBold" />

            <TextBlock Text="Kalendarz Trenera" 
                       FontSize="24" 
                       FontWeight="Bold"
                       Foreground="#E6F6FF" />
        </StackPanel>

        <Border Grid.Row="1"
            Background="#32404A"
            CornerRadius="10"
            BorderBrush="#9ECBFF"
            BorderThickness="3"
            Padding="20">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <CalendarView Grid.Column="0"
                              x:Name="MainCalendar"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Top"
                              Margin="0,0,30,0"
                              DayOfWeekFormat="{}{dayofweek.abbreviated(2)}"
                              Foreground="White"
                              CalendarIdentifier="GregorianCalendar"
                              FirstDayOfWeek="Monday" />

                <StackPanel Grid.Column="1" Spacing="15" VerticalAlignment="Top">
                    <TextBlock Text="Wydarzenia na wybrany dzień" 
                               FontSize="20" 
                               FontWeight="Bold"
                               Foreground="#CDE6F8" />

                    <Button x:Name="AddEventButton"
                            Content="+ Dodaj Wydarzenie"
                            Click="AddEventButton_Click"
                            Background="#86A9C8"
                            Foreground="#2F4450"
                            FontWeight="Bold"
                            CornerRadius="8"
                            Padding="20,10"
                            HorizontalAlignment="Left" />

                </StackPanel>
            </Grid>
        </Border>

        <Frame x:Name="ModalFrame" 
               Grid.RowSpan="2" 
               Background="#80000000" 
               Visibility="Collapsed" />
    </Grid>
</Page>