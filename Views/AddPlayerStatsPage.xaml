<Page
    x:Class="SoccerLink.Views.AddPlayerStatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:SoccerLink.Views"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CounterButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="#4A5D69"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="CounterTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E6F6FF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#86A9C8"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Page.Resources>

    <Grid Background="#405861" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <Button Content="⟵ Anuluj" Click="CancelButton_Click" Background="Transparent" BorderThickness="0" Foreground="#CDE6F8" FontWeight="SemiBold"/>
            <TextBlock Text="Uzupełnij Statystyki Indywidualne" FontSize="24" FontWeight="Bold" Foreground="#E6F6FF" Margin="0,5,0,0"/>
            <TextBlock x:Name="MatchTitleText" Text="Mecz: ---" Foreground="#FFD700" FontSize="14" Margin="0,2,0,0"/>

            <ToggleSwitch x:Name="GlobalCleanSheetSwitch" 
                          OffContent="Czyste Konto: NIE" 
                          OnContent="Czyste Konto: TAK"
                          Foreground="#E6F6FF"
                          Margin="0,15,0,0"
                          HorizontalAlignment="Left"/>
        </StackPanel>

        <ListView x:Name="PlayersStatsList" Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PlayerStatsEntry">
                    <Border Background="#32404A" Margin="0,6" Padding="12" CornerRadius="8" BorderBrush="#5A707A" BorderThickness="1">
                        <Grid RowSpacing="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0">
                                <TextBlock Text="{x:Bind FullName}" Foreground="#E6F6FF" FontWeight="Bold" FontSize="18"/>
                                <TextBlock Text="{x:Bind Position}" Foreground="#86A9C8" FontSize="12" FontStyle="Italic"/>
                            </StackPanel>

                            <Grid Grid.Row="1" ColumnSpacing="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Gole" Style="{StaticResource SectionHeaderStyle}" Foreground="#FFD700"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecGoal" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind Goals, Mode=OneWay}" Style="{StaticResource CounterTextStyle}" Foreground="#FFD700"/>
                                        <Button Content="+" Click="IncGoal" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Strzały" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecShot" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind Shots, Mode=OneWay}" Style="{StaticResource CounterTextStyle}"/>
                                        <Button Content="+" Click="IncShot" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Celne" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecShotOn" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind ShotsOnTarget, Mode=OneWay}" Style="{StaticResource CounterTextStyle}"/>
                                        <Button Content="+" Click="IncShotOn" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Niecelne" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecShotOff" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind ShotsOffTarget, Mode=OneWay}" Style="{StaticResource CounterTextStyle}"/>
                                        <Button Content="+" Click="IncShotOff" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Row="2" ColumnSpacing="20" BorderBrush="#4A5D69" BorderThickness="0,1,0,0" Padding="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Celne Podania" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecPass" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind Passes, Mode=OneWay}" Style="{StaticResource CounterTextStyle}"/>
                                        <Button Content="+" Click="IncPass" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Faule" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Button Content="-" Click="DecFoul" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                        <TextBlock Text="{x:Bind Fouls, Mode=OneWay}" Style="{StaticResource CounterTextStyle}"/>
                                        <Button Content="+" Click="IncFoul" Tag="{x:Bind}" Style="{StaticResource CounterButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                    <TextBlock Text="Kartki" Style="{StaticResource SectionHeaderStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <Button Content="-" Click="RemoveYellow" Tag="{x:Bind}" Width="30" Height="30" Padding="0" Background="#D4AC0D" Foreground="Black" FontWeight="Bold" BorderThickness="0"/>

                                            <TextBlock Text="{x:Bind YellowCards, Mode=OneWay}" Foreground="White" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>

                                            <Button Content="+" Click="AddYellow" Tag="{x:Bind}" Width="30" Height="30" Padding="0" Background="#D4AC0D" Foreground="Black" FontWeight="Bold" BorderThickness="0"/>
                                        </StackPanel>

                                        <CheckBox IsChecked="{x:Bind HasRedCard, Mode=TwoWay}" 
                                                  Content="Czerwona" 
                                                  Foreground="#E74C3C" 
                                                  MinWidth="0" 
                                                  Click="RedCardCheckBox_Click" 
                                                  Tag="{x:Bind}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="2" Content="Zapisz Komplet Statystyk" Click="SaveButton_Click" HorizontalAlignment="Center" Background="#FFD700" Foreground="#2F4450" FontWeight="Bold" FontSize="16" CornerRadius="8" Padding="40,12" Margin="0,15,0,0"/>
    </Grid>
</Page>