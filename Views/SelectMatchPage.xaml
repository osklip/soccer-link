<Page
    x:Class="SoccerLink.Views.SelectMatchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SoccerLink.Models"
    mc:Ignorable="d">

    <Grid Background="#405861" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,20">
            <Button x:Name="BackButton" Content="⟵ Powrót" Click="BackButton_Click" 
                    Background="Transparent" BorderThickness="0" FontSize="18" Foreground="#CDE6F8" FontWeight="SemiBold" />

            <TextBlock Text="Wybierz mecz do uzupełnienia" FontSize="24" FontWeight="Bold" Foreground="#E6F6FF"/>
            <TextBlock Text="Poniższe mecze nie mają jeszcze kompletnych statystyk." FontSize="14" Foreground="#CDE6F8" Opacity="0.8"/>
        </StackPanel>

        <ListView x:Name="MatchesListView" Grid.Row="1" SelectionChanged="MatchesListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Mecz">
                    <Border Background="#32404A" CornerRadius="8" Margin="0,4" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{x:Bind DataRozpoczecia}" Foreground="#86A9C8" VerticalAlignment="Center" Margin="0,0,20,0" FontWeight="Bold"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Mecz przeciwko:" FontSize="12" Foreground="#CDE6F8" Opacity="0.6"/>
                                <TextBlock Text="{x:Bind Przeciwnik}" FontSize="18" Foreground="#E6F6FF" FontWeight="Bold"/>
                            </StackPanel>

                            <FontIcon Grid.Column="2" Glyph="&#xE70F;" FontFamily="Segoe MDL2 Assets" Foreground="#FFD700" FontSize="20"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>