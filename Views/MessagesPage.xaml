<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoccerLink.Views.MessagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoccerLink.Views"
    xmlns:models="using:SoccerLink.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#405861">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="16" CornerRadius="20" Background="#86A9C8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="8,8,8,4">
                    <Button x:Name="BackButton" Content="⟵ Powrót" Click="BackButton_Click" HorizontalAlignment="Left" Padding="8,4" Background="#2F4450" Foreground="#CDE6F8" FontWeight="SemiBold" BorderThickness="0" />
                    <Button x:Name="NewMessageButton" Content="+ Nowa" Click="NewMessageButton_Click" HorizontalAlignment="Right" Padding="8,4" Background="#2F4450" Foreground="#CDE6F8" FontWeight="SemiBold" BorderThickness="0" />
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,0,8,4" VerticalAlignment="Center" Spacing="8">
                    <TextBlock Text="Pokaż:" VerticalAlignment="Center" Margin="0,0,4,0" Foreground="#2F4450" FontWeight="Bold" />
                    <ComboBox x:Name="MessagesFilterComboBox" Width="140" Background="#BFBFBF" CornerRadius="8" SelectionChanged="MessagesFilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="Odebrane" Tag="received" IsSelected="True" />
                        <ComboBoxItem Content="Wysłane" Tag="sent" />
                    </ComboBox>
                </StackPanel>

                <ListView Grid.Row="2"
                          x:Name="MessagesList"
                          ItemsSource="{x:Bind ViewModel.FilteredMessages, Mode=OneWay}"
                          SelectionMode="Single"
                          SelectionChanged="MessagesList_SelectionChanged"
                          Background="#2F4450"
                          BorderThickness="0"
                          Margin="4,4,4,8">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Wiadomosc">
                            <StackPanel Padding="8,4">
                                <TextBlock Text="{x:Bind Temat}" FontWeight="SemiBold" Foreground="#CDE6F8" />
                                <TextBlock Text="{x:Bind NadawcaNazwa}" FontSize="12" Foreground="#CDE6F8" Opacity="0.7" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Column="1" Margin="16" CornerRadius="20" Background="#86A9C8">
            <Grid Padding="32,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#2F4450" CornerRadius="22" Padding="14,10" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Do:" FontWeight="Bold" Foreground="#CDE6F8"/>
                        <TextBlock Text="{x:Bind ViewModel.SelectedMessage.TypOdbiorcy, Mode=OneWay}" Foreground="#CDE6F8"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="#2F4450" CornerRadius="22" Padding="14,10" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Od:" FontWeight="Bold" Foreground="#CDE6F8"/>
                        <TextBlock Text="{x:Bind ViewModel.SelectedMessage.NadawcaNazwa, Mode=OneWay}" Foreground="#CDE6F8"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Background="#2F4450" CornerRadius="22" Padding="14,10" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Temat:" FontWeight="Bold" Foreground="#CDE6F8"/>
                        <TextBlock Text="{x:Bind ViewModel.SelectedMessage.Temat, Mode=OneWay}" Foreground="#CDE6F8"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="3" Background="#2F4450" CornerRadius="22" Padding="16,12" Margin="0,0,0,0" VerticalAlignment="Stretch">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{x:Bind ViewModel.SelectedMessage.Tresc, Mode=OneWay}" TextWrapping="Wrap" Foreground="#CDE6F8" VerticalAlignment="Top"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>