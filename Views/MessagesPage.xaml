<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoccerLink.Views.MessagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:SoccerLink.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="16,20,16,10" Spacing="10">
                    <Button x:Name="BackButton" Content="⟵ Powrót" Click="BackButton_Click" Style="{StaticResource BackButtonStyle}"/>
                    <TextBlock Text="Skrzynka" Style="{StaticResource HeaderTextStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="16,0,16,10">
                    <Button x:Name="NewMessageButton" Content="+ Nowa wiadomość" Click="NewMessageButton_Click" 
                            HorizontalAlignment="Stretch" Background="{StaticResource TextAccentBrush}" Foreground="Black" FontWeight="Bold" CornerRadius="6" Padding="10"/>

                    <ComboBox x:Name="MessagesFilterComboBox" SelectionChanged="MessagesFilterComboBox_SelectionChanged" 
                              Margin="0,10,0,0" HorizontalAlignment="Stretch" SelectedIndex="0">
                        <ComboBoxItem Content="Odebrane" Tag="received" />
                        <ComboBoxItem Content="Wysłane" Tag="sent" />
                    </ComboBox>
                </StackPanel>

                <ListView Grid.Row="2" x:Name="MessagesList" ItemsSource="{x:Bind ViewModel.FilteredMessages, Mode=OneWay}"
                          SelectionMode="Single" SelectionChanged="MessagesList_SelectionChanged" Padding="10">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Wiadomosc">
                            <Grid Padding="10" Margin="0,2" CornerRadius="6" Background="#405861">
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Temat}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind NadawcaNazwa}" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Padding="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="5" Margin="0,0,0,20">
                <TextBlock Text="{x:Bind ViewModel.SelectedMessage.Temat, Mode=OneWay}" Style="{StaticResource HeaderTextStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <TextBlock Text="Od:" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.SelectedMessage.NadawcaNazwa, Mode=OneWay}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
                <ScrollViewer>
                    <TextBlock Text="{x:Bind ViewModel.SelectedMessage.Tresc, Mode=OneWay}" 
                               TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" FontSize="16"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>