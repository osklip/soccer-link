<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoccerLink.Views.EditEventPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SoccerLink.Models"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Border Width="600" CornerRadius="16" Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1"
                HorizontalAlignment="Center" VerticalAlignment="Center" Padding="24">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="16">

                    <TextBlock Text="Edycja Wydarzenia" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>

                    <TextBlock x:Name="EventInfoTextBlock" HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Opacity="0.8"/>

                    <StackPanel Spacing="5">
                        <TextBlock Text="Typ Wydarzenia (nieedytowalny)" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox x:Name="EventTypeTextBox" IsReadOnly="True" Background="#4A5D69" BorderThickness="0" CornerRadius="8" Padding="10,6" Opacity="0.7"/>
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <TextBlock x:Name="TitleLabel" Text="Nazwa / Przeciwnik" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox x:Name="TitleTextBox" PlaceholderText="Wpisz nazwę" />
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <TextBlock Text="Data" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <CalendarDatePicker x:Name="DateDatePicker" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <TextBlock Text="Miejsce" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox x:Name="MiejsceTextBox" PlaceholderText="Miejsce wydarzenia"/>
                    </StackPanel>

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="5">
                            <TextBlock Text="Start (HH:MM)" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBox x:Name="TimeStartTextBox" PlaceholderText="HH:MM"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="5" x:Name="TimeEndPanel">
                            <TextBlock Text="Koniec (HH:MM)" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBox x:Name="TimeEndTextBox" PlaceholderText="HH:MM"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel x:Name="OpisPanel" Spacing="5" Visibility="Collapsed">
                        <TextBlock Text="Opis" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBox x:Name="OpisTextBox" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20" Margin="0,10,0,0">
                        <Button Content="Anuluj" Click="CancelButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <Button x:Name="SaveButton" Content="Zapisz zmiany" Click="SaveButton_Click" 
                                Background="{StaticResource TextAccentBrush}" Foreground="Black" FontWeight="Bold" Padding="20,5"/>
                    </StackPanel>

                    <TextBlock x:Name="StatusTextBlock" Foreground="#E74C3C" HorizontalAlignment="Center" Margin="0,8,0,0"/>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>