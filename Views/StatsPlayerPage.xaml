<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoccerLink.Views.StatsPlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SoccerLink.Models"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center" HorizontalAlignment="Left">
                <Button x:Name="BackButton" Click="BackButton_Click" Style="{StaticResource BackButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="⟵" FontSize="20" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                        <TextBlock Text="Powrót" FontSize="20" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                <Button x:Name="HomeButton" Click="HomeButton_Click" Background="Transparent" BorderThickness="0" Padding="4">
                    <FontIcon Glyph="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Button>
            </StackPanel>

            <TextBlock Text="Ranking Zawodników" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center" FontStyle="Italic"/>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
            <ListView x:Name="PlayersListView" SelectionMode="Single" SelectionChanged="PlayersListView_SelectionChanged" Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Zawodnik">
                        <Grid Padding="12" Margin="0,4,0,4" Background="#2F4450" CornerRadius="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <FontIcon Glyph="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,16,0"/>

                            <TextBlock Grid.Column="1" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center">
                                <Run Text="{x:Bind Imie}"/> <Run Text="{x:Bind Nazwisko}"/>
                            </TextBlock>

                            <TextBlock Grid.Column="2" Text="{x:Bind Pozycja}" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>